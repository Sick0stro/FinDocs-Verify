# Large binary files → Git LFS
# Model files excluded from repo — train locally with: make train
# models/**  tracked by Git LFS if you have budget:
# models/**/*.safetensors filter=lfs diff=lfs merge=lfs -text
# models/**/*.bin filter=lfs diff=lfs merge=lfs -text
# models/**/*.ot filter=lfs diff=lfs merge=lfs -text
